<nav class="flex items-center justify-between flex-wrap bg-zinc-800 p-6 sticky top-0 z-99 w-full">
  <div class="flex flex-row gap-8 text-white">
    <a href="/">Home</a>
  </div>
  
  <% if(locals.logged_in_user){ %>
    <%- include('navbar_element_logged_in') %>
  <% } else{ %>
    <%- include('navbar_element_guest') %>
  <% } %>

</nav> 

