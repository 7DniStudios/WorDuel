<nav class="bg-gray-100 border-b-2 border-black">
  <div class="mx-auto flex h-16 max-w-7xl items-center gap-8 px-4 sm:px-6 lg:px-8">
    
    <a class="block text-teal-600" href="/">
      <span class="sr-only">Home</span>
      <img 
        src="/assets/7DaysLate.jpg" 
        alt="Logo WorDuel" 
        class="h-8 w-auto" 
      />
    </a>

    <div class="flex flex-1 items-center justify-end md:justify-between">
      <nav aria-label="Global" class="hidden md:block">
        <ul class="flex items-center gap-6 text-sm">
          <li>
            <a class="font-black font-bold transition hover:text-gray-500/75" href="/"><u> Home</u></a>
          </li>
        </ul>
      </nav>

      <div class="flex items-center gap-4 sm:flex sm:gap-4">
        <% if(locals.logged_in_user){ %>
          <%- include('user_drop_menu') %>
        <% } else{ %>
          <%- include('redirect_button', { text: "REGISTER", redirectUrl: "/auth/register" }) %>
          <%- include('redirect_button', { text: "LOGIN", redirectUrl: "/auth/login" }) %>
        <% } %>
      </div>
    </div>
  </div>
</nav>
